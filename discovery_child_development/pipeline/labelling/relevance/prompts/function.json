{
  "name": "predict_relevance",
  "description": "Predict whether the text is relevant to child development (age 0-5)",
  "parameters": {
    "type": "object",
    "properties": {
      "prediction": {
        "type": "string",
        "enum": ["Relevant", "Not-relevant", "Not-specified"],
        "description": "The predicted relevance to child development from the given text. Infer this from the provided category descriptions."
      }
    },
    "required": ["prediction"]
  }
}
