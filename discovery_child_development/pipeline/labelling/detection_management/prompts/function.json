{
  "name": "predict_detection_management",
  "description": "Predict whether the text is about detecting/anticipating child development or managing it, or both",
  "parameters": {
    "type": "object",
    "properties": {
      "prediction": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["Detecting", "Managing"]
        },
        "description": "The predicted categories. Infer this from the provided category descriptions."
      }
    },
    "required": ["prediction"]
  }
}
