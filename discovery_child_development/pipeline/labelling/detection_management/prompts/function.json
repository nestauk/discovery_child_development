{
  "name": "predict_detection_management",
  "description": "Predict whether the text is about detection/anticipation of child development or the management of it, or both",
  "parameters": {
    "type": "object",
    "properties": {
      "prediction": {
        "type": "string",
        "enum": ["Detection", "Management", "Both", "None"],
        "description": "The predicted categories. Infer this from the provided category descriptions."
      }
    },
    "required": ["prediction"]
  }
}
