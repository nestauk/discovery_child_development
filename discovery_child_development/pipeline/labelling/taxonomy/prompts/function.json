{
  "name": "predict_labels",
  "description": "Predict category labels to the given text",
  "parameters": {
    "type": "object",
    "properties": {
      "prediction": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": []
        },
        "description": "Predicted labels to the given text"
      }
    },
    "required": ["prediction"]
  }
}
