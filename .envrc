#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "discovery_child_development")"
[ "$CONDA_DEFAULT_ENV" = "discovery_child_development" ] || echo "Have you run \`make install\`?"
